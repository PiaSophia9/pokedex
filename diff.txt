diff --git a/index.html b/index.html
index 3a55008..bd83a7c 100644
--- a/index.html
+++ b/index.html
@@ -10,7 +10,7 @@
     <link rel="icon" type="image/x-icon" href="img/pokeball.png" />
   </head>
 
-  <body onload="fetchAndDisplayMiniCards()">
+  <body onload="fetchAndDisplayMiniCards(), fruitsX()">
     <header>
       <div class="logo_and_label" onclick="fetchAndDisplayMiniCards()">
         <img class="pokeball" src="img/pokeball.png" alt="" />
@@ -21,7 +21,7 @@
 
     <div id="mainContainer"></div>
     <div class="section_button" id="sectionButton">
-      <button onclick="loadAndPush20MorePokemon()">Lade mehr</button>
+      <button onclick="render20MorePokemonX(i)">Lade mehr</button>
     </div>
 
     <div id="popupBackground" onclick="closePopup()" class="card_background d_none"></div>
diff --git a/script.js b/script.js
index cb28517..d100ea1 100644
--- a/script.js
+++ b/script.js
@@ -107,9 +107,9 @@ function getBackgroundColor(currentPokemonCategory) {
 
 // Popup
 
-async function showPopup(i) {
+async function showPopup(i, backgroundColor, currentPokemonName, currentPokemonCategory, currentPokemonImageSrc) {
   document.getElementById("popupBackground").classList.remove("d_none");
-  await loadInfo(i);
+  await loadInfo(i, backgroundColor, currentPokemonName, currentPokemonCategory, currentPokemonImageSrc);
 
   renderChart();
   disableScroll();
@@ -132,8 +132,12 @@ function renderPopup(i, backgroundColor, currentPokemonName, currentPokemonCateg
 </div>`;
 }
 
-async function loadInfo(i) {
-  let url = `https://pokeapi.co/api/v2/pokemon/${pokemonNames[i]}`;
+let firstIndex = 0;
+let lastIndex = 20;
+
+// loadInfoForPopupAndRenderPopup
+async function loadInfo(i, backgroundColor, currentPokemonName, currentPokemonCategory, currentPokemonImageSrc) {
+  let url = `https://pokeapi.co/api/v2/pokemon/${allpokemonNames[i].slice(firstIndex, lastIndex)}`;
   let response = await fetch(url);
   let pokemon = await response.json();
   let name = pokemon["name"];
@@ -146,6 +150,17 @@ async function loadInfo(i) {
   renderPopup(i, color, name, category, image);
 }
 
+function render20MorePokemonX() {
+  firstIndex = firstIndex + 20;
+  lastIndex = lastIndex + 20;
+  loadInfo();
+}
+
+function fruitsX() {
+  let targetFruit = fruits.slice(0, 10);
+  console.log(targetFruit);
+}
+
 function getColor(currentPokemonCategory) {
   if (currentPokemonCategory == "grass") {
     return "#48D0B0";
@@ -288,6 +303,8 @@ async function loadInfoOf1PokemonofLast20(i, last20) {
 
 // search
 
+// 1. Das sollte der Anfang sein.
+
 async function fetchAllPokemonNames() {
   let info = await loadAllPokemonInfos();
   pushAllPokemonNamesIntoArray(info);
@@ -309,6 +326,29 @@ async function pushAllPokemonNamesIntoArray(infoAllpokemons) {
   }
 }
 
+// 2. Jetzt sollten die Daten von den ersten 20 Pokemon geladen werden und es sollte auf allpokemonNames[i] zugegriffen werden.
+
+async function loadInfoX(i, backgroundColor, currentPokemonName, currentPokemonCategory, currentPokemonImageSrc) {
+  let url = `https://pokeapi.co/api/v2/pokemon/${pokemonNames[i]}`;
+  let response = await fetch(url);
+  let pokemon = await response.json();
+  let name = pokemon["name"];
+  let category = pokemon["types"]["0"]["type"]["name"];
+  let image = pokemon["sprites"]["other"]["official-artwork"]["front_default"];
+  let pokemonStats = pokemon["stats"];
+  let color = getColor(category); // Bis hierhin bekommt das Programm alles doppelt.
+  loadStatsNames(pokemonStats);
+  loadStatsValue(pokemonStats);
+  renderMiniCardX(i, color, name, category, image, pokemonStats);
+}
+
+function fruitsX() {
+  let targetFruit = fruits.slice(0, 10);
+  console.log(targetFruit);
+}
+
+// 3. Ersten 20 Pokemon sollen dargestellt werden.
+
 function filterNames() {
   let search = document.getElementById("search").value;
   search = search.toLowerCase();
@@ -355,3 +395,46 @@ async function loadInfoFilteredPokemon(filteredPokemon) {
   console.log("indexOfNameInArrayALLPOKEMONNAMES: ", indexOfNameInArrayALLPOKEMONNAMES);
   document.getElementById("mainContainer").innerHTML += generateMiniCard(indexOfNameInArrayALLPOKEMONNAMES, color, name, category, image);
 }
+
+let fruits = [
+  "Apfel",
+  "Banane",
+  "Orange",
+  "Erdbeere",
+  "Blaubeere",
+  "Himbeere",
+  "Kiwi",
+  "Ananas",
+  "Mango",
+  "Pfirsich",
+  "Traube",
+  "Kirsche",
+  "Zitrone",
+  "Limette",
+  "Melone",
+  "Wassermelone",
+  "Pflaume",
+  "Birne",
+  "Granatapfel",
+  "Feige",
+  "Aprikose",
+  "Mandarine",
+  "Nektarine",
+  "Passionsfrucht",
+  "Guave",
+  "Papaya",
+  "Cantaloupe",
+  "Honeydew",
+  "Litschi",
+  "Physalis",
+  "Cranberry",
+  "Heidelbeere",
+  "Brombeere",
+  "Kaktusfeige",
+  "Dattel",
+  "Rosine",
+  "Kokosnuss",
+  "Avocado",
+  "Kumquat",
+  "Pomelo",
+];
diff --git a/script2.js b/script2.js
index 1aff7c2..1e42f03 100644
--- a/script2.js
+++ b/script2.js
@@ -66,3 +66,28 @@ async function loadPokemonInfo() {
 //     return "#FFD757";
 //   }
 // }
+
+async function loadInfo(i, backgroundColor, currentPokemonName, currentPokemonCategory, currentPokemonImageSrc) {
+  let url = `https://pokeapi.co/api/v2/pokemon/${allpokemonNames[i].slice(firstIndex, lastIndex)}`;
+  let response = await fetch(url);
+  let pokemon = await response.json();
+  let name = pokemon["name"];
+  let category = pokemon["types"]["0"]["type"]["name"];
+  let image = pokemon["sprites"]["other"]["official-artwork"]["front_default"];
+  let pokemonStats = pokemon["stats"];
+  let color = getColor(category); // Bis hierhin bekommt das Programm alles doppelt.
+  loadStatsNames(pokemonStats);
+  loadStatsValue(pokemonStats);
+  renderPopup(i, color, name, category, image);
+}
+
+function render20MorePokemonX() {
+  firstIndex = firstIndex + 20;
+  lastIndex = lastIndex + 20;
+  loadInfo();
+}
+
+function fruitsX() {
+  let targetFruit = fruits.slice(0, 10);
+  console.log(targetFruit);
+}
